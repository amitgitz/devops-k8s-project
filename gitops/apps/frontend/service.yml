apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: devops
  labels:
    app: frontend
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
